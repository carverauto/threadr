apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: carverauto-issuer
spec:
  acme:
    solvers:
    - dns01:
        cloudflare:
          apiTokenSecretRef:
            name: cloudflare-api-token-secret
            key: api-token
