apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: default-farm-certificate
  namespace: default
spec:
  secretName: default-farm-threadr-ai-tls
  issuerRef:
    name: threadr-issuer
    kind: ClusterIssuer
  commonName: farm.threadr.ai
  dnsNames:
  - tunnel.threadr.ai
  - '*.tunnel.threadr.ai'

